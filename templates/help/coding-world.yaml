---
# Coding World Help (Canonical)
# Version: 1.2.0
# Constitution: §III.I Software Development

command: "dirforge init coding | dirforge create coding"
world_type: "CODING_WORLD"
schema_version: "1.2.0"
type: "world-help"
category: "world"

syntax: |
  dirforge init coding --language <lang> --project "name" [options]
  dirforge create coding --language <lang> --project "name" [options]
  dirforge init coding --help
  dirforge create coding --help

short_help:
  summary: "Create coding projects with language-aware scaffolding, tests, and docs"
  usage: "dirforge create coding --language python --project \"project-name\""

long_help:
  summary: |
    CODING_WORLD help is canonical for coding project creation across both init and
    create command paths. It defines language-specific defaults, naming rules, and
    reproducible directory layout so generated projects remain consistent.
  details: |
    Use this help file as the single source of truth for coding-world semantics.
    Compatibility aliases such as create-coding.yaml point here and should remain
    minimal during migration.
  important_notes:
    - Python and Fortran can bootstrap conda environments.
    - Docker options are only applied when language is docker.
    - Project names are normalized to snake_case IDs.

description: |
  Creates standardized software development projects with version control,
  testing, and documentation structure for multiple programming languages.
  Supports Python, MATLAB, Fortran, Bash, Rust, and Docker with language-specific tooling.

sections:
  required_options:
    title: "Required Options"
    content: |
      --language <lang>              Programming language (python|matlab|fortran|bash|rust|docker)
      --project "name"               Project name (converted to snake_case)
  
  optional_options:
    title: "Optional Options"
    content: |
      --python 3.11                  Python version for conda (Python only, default: 3.11)
      --no-conda                     Skip conda environment creation
      --no-git                       Skip Git repository initialization
      --docker-bootstrap             Create Docker dev .env and helper scripts (Docker only)
      --docker-base-image <image>    Docker base image (Docker only, default: alpine:3.20)
      --docker-port <port>           Container/service port (Docker only, default: 8080)
      --docker-service <name>        Compose service name (Docker only, default: app)
  
  supported_languages:
    title: "Supported Languages"
    content: |
      python                         Python package with setuptools, pytest, conda
      matlab                         MATLAB project with standard directory structure
      fortran                        Fortran project with Makefile and build system
      bash                           Bash scripting project with modular structure
      rust                           Rust project with Cargo.toml and src/main.rs
      docker                         Docker project with Dockerfile and compose setup
  
  naming_conventions:
    title: "Naming Conventions"
    content: |
      Project ID Format:             <snake_case_project_name>
      Example Input:                 "Data Analysis Tool"
      Generated ID:                  data_analysis_tool
      Conda Environment:             coding_data_analysis_tool (Python/Fortran)
      Rules:                         ASCII lowercase [a-z0-9_-] only
  
  directory_structures:
    title: "Directory Structures"
    content: |
      CODING_WORLD/<language>/<project_id>/
      ├── .integrity/                Language/project metadata and validation
      ├── src/                       Source code
      ├── tests/                     Test files
      ├── docs/                      Documentation
      ├── Cargo.toml                 Rust package manifest (Rust)
      ├── Dockerfile                 Container build specification (Docker)
      ├── docker-compose.yml         Multi-container orchestration (Docker)
      ├── environment.yaml           Conda environment (Python/Fortran)
      ├── pyproject.toml             Python packaging (Python)
      ├── Makefile                   Build system (Fortran)
      └── .gitignore                 Version control exclusions

examples:
  - title: "Basic Python project"
    command: 'dirforge create coding --language python --project "data_processor"'
    description: "Creates Python project with conda environment"
  
  - title: "Python project with specific version"
    command: 'dirforge create coding --language python --project "ml_toolkit" --python 3.12'
    description: "Creates Python project with Python 3.12"
  
  - title: "Python without conda"
    command: 'dirforge create coding --language python --project "simple_script" --no-conda'
    description: "Creates Python project without environment"
  
  - title: "MATLAB project"
    command: 'dirforge create coding --language matlab --project "signal_processing"'
    description: "Creates MATLAB project structure"
  
  - title: "Fortran project"
    command: 'dirforge create coding --language fortran --project "numerical_solver"'
    description: "Creates Fortran project with build system"
  
  - title: "Bash scripting project"
    command: 'dirforge create coding --language bash --project "backup_script"'
    description: "Creates bash project with modular structure"

  - title: "Rust project"
    command: 'dirforge create coding --language rust --project "claude"'
    description: "Creates Rust project with Cargo manifest and tests"

  - title: "Docker project"
    command: 'dirforge create coding --language docker --project "llama"'
    description: "Creates Docker project with Dockerfile and compose setup"

  - title: "Docker project with custom settings"
    command: 'dirforge create coding --language docker --project "llama" --docker-base-image python:3.12-slim --docker-port 5000 --docker-service api --docker-bootstrap'
    description: "Creates Docker project with custom image/service/port and bootstrap scripts"

error_handling:
  description: |
    The command provides detailed error messages with examples when required 
    arguments are missing.
  examples:
    - missing_project: 'dirforge create coding --language matlab'
    - missing_language: 'dirforge create coding --project "my_tool"'
    - missing_both: 'dirforge create coding --python 3.12'

related_commands:
  - "dirforge create research"
  - "dirforge create lecture"
  - "dirforge init"

see_also:
  - "Constitution §III.I - Software Development Projects"
  - "docs/yaml-config-user-guide.md - Configuration system"
  - "docs/docker-coding-projects.md - Docker coding project customization"

constitution_section: "§III.I"
updated: "2026-06-19"
